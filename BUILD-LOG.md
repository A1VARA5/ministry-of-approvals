# Build log (honest, timestamped, both paths)

## 2026-09-19

- 13:17 Folder created, research agents dispatched (4), 550 docs pages scraped. Sanity Context enabled on the org (Labs). CLI login done by Aivaras. Sanity MCP wired into Claude Code with a Bearer token from the CLI config because `sanity mcp configure` wrote the entry without auth and this session predates it.
- 15:xx Ideas decided: Bot Lawyer (Path One) and The Ministry of Approvals (Path Two). Trading and work data ruled out by Aivaras.
- 15:xx Research agent on Discord sources: verdict yes. June 10 2026 threshold change (100 servers to 10,000 users) left half the docs stale. 10 contradictions with verbatim quotes in bot-lawyer/research/CONTRADICTIONS.md. Corpus snapshotted to research/raw (160 files).
- Secrets received from Aivaras (DEV key, Sanity org robot token `bot-lawyer-context`, Discord webhook, Anthropic test key) and stored in `.secrets/` (gitignored). They were pasted in chat, so all four go on the rotate list before the agent session is uploaded.
- `npx sanity@latest init --yes --project v6745vem --dataset production --template clean --typescript --output-path studio` in bot-lawyer/. Clean. Studio 6.15.
- Schema written: policyArea, source, rule, question. Key modelling decisions: rules quote verbatim and carry `appliesWhen` (users, servers, intents, verification state, region, owner age) plus `effectiveFrom/To`; sources carry an `authority` number because Discord staff said support articles reflect user functionality, not bots; `conflictsWith` and `resolution` on rules mirror Context Instructions. `tsc` clean. `npx sanity schema deploy`: 1/1 deployed.
- Studio deploy: first attempt failed on `studioHost` missing (set `studioHost: 'bot-lawyer'` in sanity.cli.ts). Second attempt failed on icon imports: `@sanity/icons` v5 has no named exports from the index, you import per icon (`@sanity/icons/Tag`). The best practices reference showed exactly that and I did not read it closely enough. Fixed all four schema files. Deployed: https://bot-lawyer.sanity.studio (appId npz9n17h48jegscbqf5j1s5o). GROQ mode on Context requires a deployed Studio, not only a deployed schema.
- Knowledge base created via CLI (`npx sanity context create`), id kbyycNCGyegi. First imports: live crawl of docs.discord.com/developers (177 pages) + ZIP of 84 snapshot files. Build refused: "The organization's plan limit for indexed documents is reached (303 of 150 used)". So the cap is 150 indexed documents per org on this plan. Deleted both imports, curated a ZIP of 80 files (26 dev docs pages, 32 developer support articles, 11 user support legal add ons, 3 legal pages, 8 GitHub issues) and imported that instead. Every snapshot file carries its source URL and dates in a header so citations still resolve. Build started 14:38.
- 14:38 to 14:48 KB build 1 ran 10 minutes then failed: "The outline's structure stopped converging at "thin_leaves" (1 remaining) after 3 round(s). Rebuilding resumes from the saved progress." Restarted build 2 at 15:0x. The message says it resumes, so this should be shorter.
- Created MCP endpoint `bot-lawyer-rules` in the Context dashboard (GROQ mode, dataset v6745vem.production, GROQ filter `_type in ["rule","source","policyArea"]` so the agent cannot read the eval questions). The Name field auto slugs from Title and appends what you type, so I had to clear it. Verified with the org token: `/status` returns requiredOk true, schema 3 document types, tools/list returns initial_context, groq_query, schema_explorer, array_field_reader.
- Web app (bot-lawyer/web): Next.js 16, ai 6, @ai-sdk/mcp 1, @ai-sdk/anthropic 3, @ai-sdk/react 3, react-markdown. Two MCP clients merged, both initial_context tools dropped and their payloads inlined into the system prompt, groq_query and knowledge_base_read re-described with a routing hint. Type errors on first pass: convertToModelMessages is async in ai 6, and the close() callback had to return void. `next build` clean.
- 2026-09-19 15:xx Seed dataset (subagent). Wrote `bot-lawyer/seed/build_seed.py` (generator) and `seed/validate_seed.py` (checker), output `seed/seed.ndjson`: 10 policyArea, 47 source, 100 rule, 28 question, 185 documents, explicit readable ids (area.intents, source.gateway, rule.intent-threshold-10k-users). All 10 contradictions have both sides as rules with symmetric conflictsWith, a resolution and contradictionId; 11 supersedes chains (100 servers to 10,000 users, message content Feb to Sept 2022, retention 2017 > 2020 > 2022 > 2024, terms versions, upload limit 10 to 20 MiB, ownership transfer). Quotes: 100 of 100 found in research/raw (82 byte exact, 18 exact after unescaping markdown table underscores and stripping link targets); 15 spot checked with grep -c -F, 15 hits. No rule dropped for lack of a quote. Validator also checks unique ids, dangling refs, _key on reference arrays, schema list values, dates, no em or en dashes.
- Import: `npx sanity dataset import ../seed/seed.ndjson production --replace` from studio/: "Done! Imported 185 documents". Verified with GROQ: count rules 100, sources 47, questions 28, areas 10, zero dangling source refs. Problems: none in the import. Two small gotchas: the CLI warns that the positional dataset argument is deprecated (use --dataset), and the grep spot check first failed because a TSV written by Python on Windows carried CR line endings, fixed with tr -d '\r'.
- Dating caveats recorded in `seed/VALIDATION.md`: 21 rules quoting undated developer docs pages (rate limits, sharding, command quotas, monetisation checklist, scopes) carry effectiveFrom 2026-09-19 (snapshot) with confidence inferred, so they sort above the June 10, 2026 rules in `order(effectiveFrom desc)`; backdate from discord-api-docs git history if the agent needs earlier as of dates. Upload limit change is dated 2026-09-03 (change log label), not 2026-09-05 (GitHub issue close date); both recorded on the source. Three sources cited in CONTRADICTIONS.md are not quoted by any rule (what-are-privileged-intents, what-are-premium-apps, premium-onboarding) and are kept as citation targets.
- KB build 2 succeeded (resumed from build 1 progress, about 8 minutes). State went to "review" with 4 issues. One real Conflict found by Context on its own: the Monetization Terms page says "Effective June 6, 2024" and, lower down, "As of June 4, 2024 the Growth Tier will apply". I checked the raw file: both are on the same page, they are two different facts (document effective date vs the clause's own date). Resolved as June 4 for the Growth Tier. The dashboard said "The entry is being updated now, and saved as an instruction for future rebuilds." The other 3 issues were "merge entry" suggestions for entries with only 2 sources; the tool says that issue type is not executable yet, so I dismissed them (gateway sharding, webhooks, command registration all stand alone).
- Added a manual Instruction for the June 10 2026 intents change, scoped to the application resource page, gateway page, verification article, privileged intents articles and the change log. Saving it, Context reported "This rule also contradicts facts on" two other entries (Portal Issues: "guild count threshold requiring verification"; App Verification: "scale past 100 servers") and offered to rebuild those 2 pages. Accepted. Two Instructions now active.
- Created endpoint `bot-lawyer-kb` (KB mode) in the dashboard. Form quirk: the page is in an iframe, so the Create button was below the fold and unreachable by scroll; focused the checkbox and pressed Tab to reach it. Verified: status knowledgeBases resolved [kbyycNCGyegi], tools initial_context + knowledge_base_read, outline lists 25 entries.
- First end to end run (Haiku): trap question "90 servers, do I need Message Content approved" answered correctly (no, threshold is 10,000 users since 2026-06-10) with quote and link. But: the GROQ query guessed a KB path as an area slug, the model narrated "let me check", and it used an em dash.
- Second question (retry_after seconds vs milliseconds) exposed the real problem: Haiku skipped groq_query entirely, answered from the KB, and said "both sources agree", which is false, the dataset has contradiction C5. Three fixes: (1) prompt now mandates groq_query first and forbids "sources agree" unless the dataset shows no conflict; (2) my slug hint was wrong twice (the seed used kebab case slugs and slug is an object, so `area->slug == "rateLimits"` returned nothing); switched the hint to `area._ref == "area.rateLimits"` with the ten ids listed; (3) switched the model to claude-sonnet-5 because Haiku ignored the tool order even with the stronger prompt. Sonnet then answered with both sides of C5, dates, authority ranking and the resolution, in one tool call. UI also strips any em or en dash the model emits.
- Eval run 1 (28 questions, Sonnet, 13.4 s average, no errors, groq_query used on every question): graded by a separate agent, 23 PASS / 5 PARTIAL / 0 FAIL. 28/28 cite a URL and a date, 12/28 surface a contradiction, 0 traps fell for the stale 100 servers answer. Partials were mostly my fault: the runner never sent the developer's numbers (so q01 and q04 asked instead of ruling), my GROQ hint used title/quote filters that dropped caveat rules (q03, q12, u03), and 13 answers had an em dash in the citation line. Fixes: citation format spelled out with no dash, whole area query with no text filter, rule when numbers are given, runner appends the situation, undated pages say "page undated, snapshot 2026-09-19". Re-ran the 5 partials: all 5 now rule definitively, 0 dashes. Full eval and grades in bot-lawyer/eval/ (results.json, GRADES.md, results-partials-rerun.json).
- Aivaras opened localhost and said it looked meh. He was right: the scaffold's dark mode media query turned the page black with near black text, there was no typography plugin so the markdown was unstyled, and it was default Tailwind grey. Redesigned as a counsel's memo: fixed paper palette (so screenshots look the same on every judge's machine), Fraunces serif for headings and quotes, Inter body, JetBrains Mono for case files, a red RULING stamp, gold ruled blockquotes, sources as their own line, tool calls collapsed as "case file" entries. Also fixed a markdown quirk where the Source line got swallowed into the blockquote (prompt asks for a blank line, and the renderer inserts one if the model forgets).
- Aivaras asked for "more Discord meme". Reskinned the whole page as a Discord DM with the Bot Lawyer app: server rail, #ask-counsel channel with the anxious topic line, messages with avatars and an APP badge, every ruling rendered as a Discord embed whose left border is green for yes, red for no, yellow for depends or unresolved (parsed from the Ruling line), tool calls as ephemeral "/lookup rules dataset, Only you can see this" lines, "Bot Lawyer is typing" indicator, and the fake channels #case-files and #billing-never in the sidebar. Bug found while doing it: the case file lines never rendered in any version because MCP tools arrive as `dynamic-tool` parts in the AI SDK UI stream, not `tool-<name>`. Fixed.
- He also asked Bot Lawyer to draft a Discord formatted message and it could not, because formatting rules were never in the dataset. An agent is adding a "Formatting and limits" area (markdown syntax, message and embed limits, channel naming rules) to the rules dataset and the knowledge base, plus a drafting mode in the prompt that returns the message in a code block with the rules applied and a character count.
- "Aim higher" pass (Aivaras: fix everything you want). Added: (1) Rule as of: a date control in the header, passed to the route, the prompt rules only with rules in force on that date. Same question, two dates, two rulings: as of 2026-05-01 "Yes, 120 servers clears the threshold" (rule in force 2020-10-27 to 2026-06-09); as of today "No, 4,000 users is under 10,000, server count is irrelevant". (2) Rules on file: the rule documents each groq_query returned, rendered under the embed with effective range, applies when thresholds, source kind, authority bar, conflict badge, superseded strikethrough. Parsed from the MCP tool output, no model involved. (3) /contradictions: a public ledger page of every documented contradiction from the dataset, both sides, dates, resolution. (4) Sanity Insights: experimental_telemetry with @sanity/context/ai-sdk, org scoped client, one thread per page load; conversations now appear in the Context dashboard Insights tab (verified, "awaiting analysis" until a classifier is set up). (5) Rate limit: 6 questions per minute per IP on the route.
- Found while building the ledger: anonymous reads on dataset production return empty even though `sanity datasets visibility get` and the datasets API both say public (aclMode public), and the Manage security page says "Publicly readable: production". Doc fetch says omitted reason "permission". Toggling private then public did not change it. Docs say unauthenticated reads of a private dataset return 200 with empty results, which is what we see. Worked around with a read only viewer token on the server component. To ask in the Sanity Discord.
- Also found: `npm audit fix` (not force) had downgraded @sanity/context to 0.7.1, whose API writes conversations into a project dataset. Reinstalled ^2.0.0 for the org level Insights API.
- Plan constraint spotted: this plan includes 2 datasets, we use 1. The Ministry needs content plus workflow documents, so it will be one dataset or a second (free) project in the same org.
- 19:48 to 20:30 Formatting and limits area (subagent). New policy area `area.formatting` (title "Formatting and limits", order 11) so the lawyer can answer "why does my header not render" and draft messages and channel lists. Sources fetched through the Zendesk JSON API with research/raw/zd.py into research/raw/formatting/ (12 files, same header format): Markdown Text 101 (210298617, edited 2025-04-23), Spoiler Tags (360022320632), Sending Messages (360034632292, the 2000 and 4000 character section), What are Nitro and Nitro Basic (115000435108), Discord Account Caps, Server Caps, and More (33694251638295, edited 2026-03-02: 500 channels, 50 per category, 50 categories, 2000/4000 message length), docs.discord.com channel, guild, message and reference pages as .md with a provenance header prepended, GitHub issue 1646 (2020, the only written record that text channel names reject capitals and spaces; no Discord page states it), and, per Aivaras, two emojidb.org catalogue pages (channel name symbols, channel emojis) saved with curl and modelled as kind github with authority 20 and "community convention, not a Discord rule" in every title. Three fetched support articles (Channel Categories 101, Text Channels, Custom Emojis) had nothing quotable and were dropped. Channel Categories 101 says nothing about limits; the docs say nothing about lowercase names.
- Seed: build_seed.py grew a formatting section (same helpers, `source()` got an authority override) and now also writes seed/seed-formatting.ndjson with only the new documents: 1 area, 11 sources, 36 rules, 8 questions (q26 to q33; q32 is the welcome message draft, q33 the crypto server channel list). validate_seed.py: 241 documents, 136 of 136 quotes found (112 exact, 24 after markdown normalisation), 0 errors; the 185 existing documents are byte identical to the previous build. New contradiction C11 appended to research/CONTRADICTIONS.md: the support caps table says 50 categories per server, docs error code 30030 says 5; both sides are rules with symmetric conflictsWith and a resolution (plan for 50). Rules reuse `source.reference` for mention, emoji and timestamp syntax and `source.opcodes` for error codes 30013 and 30030 rather than duplicating those pages. Dating: support rules carry the Zendesk edit date, inferred; docs rules the snapshot date, inferred; slash command mentions 2022-08-22 from the change log.
- Import: `npx sanity dataset import ../seed/seed-formatting.ndjson production` from studio/, no --replace: "Imported 56 documents". GROQ after: areas 11, sources 58, rules 136 (36 in area.formatting), questions 36, dangling source refs 0, C11 rules 2.
- KB: description updated with `npx sanity context update` because the old one said "leave out end user help and Nitro", which would have suppressed the support articles the whole area depends on. Imported a zip of 10 new raw files (the reference and message pages were already indexed, so left out): ingest job succeeded in 6 seconds, 15 pending additions. Build 3 (`context build --watch`) ran 20:02 to 20:21, "Build succeeded" first time, no convergence failure. Sources now 90 (70 ready, 20 skipped; the skipped set is the large pages, including the new channel 86 KB and guild 132 KB docs, same as reference.md and resources_message.md were before, yet the outline still gained a channel structure entry). New outline entry `message_formatting [core]` covers markdown, mentions, timestamps, message and embed limits, channel name and count caps and the community naming convention. State went to review with 4 open issues; the CLI and the org API expose only apply/dismiss/resolve routes for issues, no GET, so the list has to be read in the dashboard. sourceUsage reports 133 of 5000.
- Prompt: web/src/lib/prompt.ts got a "# Drafting messages" section (groq_query area.formatting first, draft inside one ```md code block, "Rules applied" lines with source links, community convention marked as such, "Length" line against 2000 with the Nitro 4000 caveat, channel lists as CAPS category headers plus emoji + separator + lowercase name). context.ts groq_query description now lists eleven area ids including area.formatting. tsc clean. web/src/app untouched.
- Tests through the running dev server (eval-ask.py, Sonnet): "why does my ## header render as literal text?" answered in Ruling format in 9.4 s with one groq_query, quoting the Markdown 101 note and giving the corrected line; "Write a welcome message for my gaming server..." produced a ```md draft (header, three bullets, spoiler), four Rules applied lines with links and "231 characters, well under the 2000 character bot API limit" in 10.4 s; the crypto channel list question produced WELCOME / COMMUNITY / LIVE categories with 👋┆welcome, 📜┆rules, 📢┆announcements, 💬┆general-chat, 📈┆trading, 😂┆memes, 🔴┆live-streams, cited the 1 to 100 rule, the inferred lowercase rule from GitHub 1646 and marked the emoji and separator choice as community convention, in 18.5 s. One nit: the header answer did not say "inferred" although the rule is marked inferred (the model projected fields without confidence); the drafting query does include it.
- 21:00 to 21:30 Test pass 2 (subagent, no source changes; full report in bot-lawyer/eval/TEST-PASS-2026-09-19.md, grades in eval/GRADES-2.md). Eval run 2 with the situation block sent: 15 PASS / 3 PARTIAL / 0 FAIL of 18 answered, then the run died at q19 because the Anthropic test key ran out of credit ("Your credit balance is too low to access the Anthropic API"); the runner has no back off, so q22 to u03 hit the app's own 429. q01, q04, q12 fixed since run 1; q16 regressed to a "which region?" question; q03 still misattributes the 90 day window. Em dashes down from 13 answers to 1 (inside quoted text).
- Production smoke: GET / 200 with ask-counsel, GET /contradictions 200 with C1 to C11, POST /api/discord unsigned 401, one real question streamed in 19.9 s, seventh rapid POST got 429 with Retry-After 60. After the key died, production returns "An error occurred." in 1.5 s on every question. The public demo is down until credit is added.
- Discord path: applications/@me confirms interactions_endpoint_url https://bot-lawyer.vercel.app/api/discord and the guild command "ask" with options question and as_of; no global commands. Application flags 565248 mean all three privileged intent toggles (message content, members, presence) are on in the portal although the bot uses none of them. Turn them off.
- Mobile at 390 px (via a same origin iframe because the extension could not resize the maximised window): no horizontal overflow on / or /contradictions, sidebar collapses, embeds and Rules on file cards fit. Five screenshots in 04-submission/screenshots/. Usability catch: the first example question omits a user count, so the headline click ends with the bot asking "Roughly how many unique users does your app reach?" instead of a ruling.
- Time travel (asOf 2026-05-01 versus today) could not be verified in this pass, both calls errored on the key. Judge read: Context and structured content 4, code quality 3, Knowledge Bases 3, usability 3. Top fixes: fund the key, put a user count in the first example question, three prompt lines (no preamble, date outside the blockquote, kind as words, rule per branch instead of asking), back off in run_eval.py and rerun the 18 blanks, switch the intent toggles off.
- Test pass 2 (separate agent, TEST-PASS-2026-09-19.md): the Anthropic test key ran out of credit mid eval (400 "credit balance is too low"), so 18 of 36 questions never ran and the live demo returned "An error occurred" from about 21:05. Of the 18 answered: 15 PASS, 3 PARTIAL, 0 FAIL, 0 traps failed, em dashes down from 13 answers to 1. Production smoke, rate limit, Discord config and phone width layout all passed. Judge read: Context and structure 4, implementation 3, Knowledge Bases 3 (knowledge_base_read used in 2 of 18 answers, the GROQ dataset does most of the reasoning), usability 3 (first example question had no user count so the bot asked instead of ruling).
- Fixes after test pass 2: all three privileged intent toggles were on in the Developer Portal despite the bot not using any (flags 565248); set flags to 0 via the API so the app matches its own advice. First example question now includes a user count, a monetisation example includes a region. Prompt: nothing before Ruling, kind written as words, effective date outside the blockquote, and for two or three branch facts (region, verified or not) give each branch instead of asking. Route: a readable error when the model provider fails on billing, rate limit or outage. Eval runner: 11 s spacing and one retry after 61 s on error or 429.
- Money: the $7 test key covered two full evals, partial reruns, the formatting agent's tests and manual testing on Sonnet, then died. The public demo depends on a funded key. Options: top up, or route through Vercel AI Gateway.
- Aivaras: "so you used all of my credits?" Yes. I said I would warn at $3 and never checked the balance; I estimated and the estimate was wrong because every call carries both initial contexts in the system prompt (10 to 15 thousand input tokens) on Sonnet, and about a hundred calls ran today. Fixes shipped: Anthropic prompt caching on the system prompt (identical every call, cached reads are about a tenth of the price), per request usage logging with an estimated cost, and a daily USD cap (BOT_LAWYER_DAILY_USD, default 3) that returns a readable "retainer cap" message on both the web and Discord paths. Rule from here: no eval runs without stating the estimated cost and getting a yes.
- Key topped up by Aivaras ("use responsibly"). Shipped the three judge fixes: (1) Knowledge Bases: every ruling now runs groq_query and knowledge_base_read in the same turn, quotes must be complete, and 4 KB only eval questions were added (40 questions total). Verified on the ownership transfer question: the KB entry supplied the support ticket route, the 90 day transfer limit and the ID data deletion that the rules dataset did not hold. (2) Technical: provider fallback to Vercel AI Gateway when the Anthropic key fails on billing or rate limits (15 minute switch, Discord path retries in the same call). (3) Usability: "short answers" toggle, under 120 words. Measured cost: 8 to 10 cents per question; the cached system prompt is 17.6k tokens, but tool payloads (15 to 20 rules with quotes plus a KB entry, re read on the final step) dominate. First query now excludes superseded rules unless time travel is on, to shrink the payload. Eval of the remaining 22 questions quoted at about $2.20, awaiting a yes.
- Eval run 3 (22 questions, approved by Aivaras, $1.37 actual): 16 PASS / 5 PARTIAL / 1 FAIL. Combined final table over 40 questions: 31 PASS / 8 PARTIAL / 1 FAIL, ruling right on 39 of 40, 22 of 40 read the knowledge base (was 2 of 18), 12 surface a contradiction, 20.6 s average. One answer (k01) came back empty on the first pass: finishReason length, my 1400 token output cap was eaten by reasoning plus a long query; raised to 3000 and reran, and the rerun is the best KB answer of the set (it found the retention duty moved from the Policy to the Terms in 2022). The single FAIL (q21) is a real defect: a knowledge base summary sentence was put in a blockquote and attributed to a page that does not say it. Fixed with a hard prompt rule: blockquotes only from rule quote fields or text the KB entry marks as the page's own words; KB summaries go in prose without quote marks. Also: draft "Length" line is now computed in code (the model miscounted twice), and raw rule ids are banned from prose. Fixes deployed, not re-measured (that would cost another run).
- Packaging: repo pushed to https://github.com/A1VARA5/bot-lawyer (public, MIT, secrets scan clean, Discord page snapshots excluded from the repo, studio's own .git removed). Recorded a GIF on production: the 90 servers question today, then the same bot as of May 2026 (same verdict, different rule, both dated). Dashboard screenshots: 5 active Instructions (4 from resolved issues, 1 manual), 5 resolved, 3 dismissed, 1 pending on purpose; Insights shows 65 conversations including Aivaras's Discord tests. Applied the "rules not reflected in 2 entries" update.
- 2026-09-21 Anonymous read investigation: created a fresh public dataset `acltest` with one document. Authenticated count 13 (system docs plus probe), anonymous count 0, anonymous doc fetch "permission". Control: Sanity's own public project 3do82whm returns 25,936 anonymously. Org security page shows "2 public datasets". Conclusion: the project (created 2026-09-19 via dashboard onboarding, Growth trial) denies unauthenticated reads regardless of dataset visibility. Deleted acltest. Post updated with the precise statement; Discord message prepared for Aivaras.
- Polish: "Pages read" panel under every answer (knowledge base entries read plus their source pages, parsed from the tool output), two knowledge base shaped example questions, and the prompt now marks page derived context with "From the page:". Verified on production with the payout timing question: ruling from rules, 90 day dispute window from the page, both labelled. Draft mode on production confirmed: code block plus Length line, nothing else.
- Re ran q21, q24, k02, u02 after the blockquote provenance rule ($0.30, approved): 12 of 12 blockquotes verbatim in the raw snapshots by string match, 0 dashes. Post updated to say so; the original table left unchanged.

## 2026-09-21 Path Two: The Ministry of Approvals

- 02:2x Started Path Two. Read the Workflows docs again (effects-and-runtimes, sanity-functions, app-sdk, studio-plugin, the AI content pipeline cookbook) before writing anything. Versions on npm today: all @sanity/workflow-* at 0.33.0, @sanity/sdk 3.3.0, sanity 6.15.0, astro 7.3.3. Node 22.15.
- Dataset: the plan allows 2 and Bot Lawyer uses 1, so `ministry` (public) holds both the content and the workflow engine documents. The docs say a single dataset works; it also means the subject GDR needs no alias.
- Studio scaffolded with the clean template (`npx sanity@latest init --project v6745vem --dataset ministry`). Schema: submission (what the citizen wrote, no status field on purpose, the workflow instance is the status), department (a stage described as content: name, motto, clerk, stamp colour), clerk (an AI personality stored as content: role, temperament, systemPrompt), certificate (written by an effect, read only in Studio, copies the remarks in as stamps), receipt (written when the Archive loses a form, id from the effect key so a retry cannot issue two). Custom input on clerk.temperament: a slider that rewrites the sibling systemPrompt through useDocumentOperation. Custom document badge on submissions that reads the live workflow stage through @sanity/workflow-studio.
- Broke: `npm install` of the Workflows packages into Studio 6.15 fails. @sanity/workflow-components and -diagram 0.33.0 peer on @sanity/ui ^3.3.5, every Studio from 6.10 up ships @sanity/ui 4. Checked each minor: 6.9.x is the last line on ui 3.5. Pinned sanity 6.9.2 (still above the 6.3 minimum the plugin needs). Second failure: @sanity/workflow-blueprint wants TypeScript 6 (peerOptional) and the Studio template pins 5; moved the cli and blueprint packages into the separate workflows/ package where they belong. Dropped @sanity/vision (its 6.9.2 wants ui 4 as well). Final tree: sanity 6.9.2, @sanity/ui 3.5.5, @sanity/sdk 3.3.0 with the @sanity/mutate 0.18.2 override the docs ask for.
- Workflow `ministry-approval` written with defineWorkflow: intake (Front Desk) -> department-a (Pedantry, AI effect) -> department-b (Rubber Stamps, AI effect) -> department-c (Archive, AI effect, loses every third serial once) -> minister-review (human: approve / reject with reason / refer to a department, plus an assignee field for who is Minister) -> certified (issue-certificate effect, then announce effect that POSTs a Discord webhook with retry 3x exponential) -> on-the-wall. Withdraw at the desk ends in shredded. Any failed clerk escalates to the Minister. `sanity-workflows deploy --check` passed first time. Deployed as v1 with the CLI.
- Effect handlers: each clerk loads its clerk document by role and calls `client.agent.action.prompt` (apiVersion vX, format json, the form passed as a groq instructionParam so the model only sees title, kind, body, citizen, serial, amendments). Outputs are typed (complete, lost, remark) and the transitions read them; remarks are also written back to workflow fields with field.set ops so the status page and the certificate can show them. The rubber stamp and the archivist fall back to their catchphrase if the model call fails; the pedantic clerk does not, a failure there escalates to the Minister.
- Local drainer (workflows/src/drain-local.ts): sweeps instances with pending effects at start, then `client.listen` on instances with unclaimed effects and calls drainEffects per instance. This is the laptop stand in for the Sanity Function.
- First run (serial 1): startInstance cascaded Front Desk -> Pedantry automatically. Drainer ran the pedantic clerk in 14 s: complete=false, demand "Add the date of submission", citing an invented Regulation 4.2. Instance back at intake with the demand on it.
- Broke: fired resubmit from the CLI with the amendment as a param, Pedantry rejected again. My design put the amendment write on the website (write the doc, then fire the action), so a CLI or Studio caller never got it onto the document. Fixed in v2: resubmit parks the amendment and what it answers on workflow fields (fieldRead of demand / rejectionReason), the pedantic handler files it onto the submission document (array key from the effect key, so retries append once) and clears the fields. Now every caller behaves the same. Aborted the v1 instance.
- Serial 2 full pass on v2: Pedantry demanded a signature ("subsection 4.2"), resubmitted with reference MoA/LUNCH/11 in triplicate, one drainEffects call then went Pedantry (accepted "grudgingly") -> Rubber Stamps ("looks nicely in order, the lunch details seem very thorough") -> Archive ("Shelf L, mildly resigned, between Early Meals and Preventable Biscuit Runs") -> Minister's desk in about 20 s. Fired approve with a note from the CLI; drain issued certificate MoA-2026-0002 with three stamps and posted it to Discord. Instance completed at on-the-wall. Five Agent Actions prompt calls total for the pass.
- Noticed: effectHistory records the actor as my person id with all my roles, because the local drainer uses my project token. The Function will use a robot token so the audit trail says drainer.
- 02:55 Blueprint in workflows/: `npx sanity@latest blueprints init . --type ts --project-id v6745vem --stack-name production` (stack ST-b2rsbtitb2). sanity.blueprint.ts declares a robot token (project editor) and one Document Function `wf-prod-drain-effects` with the docs' delta filter (unclaimed pendingEffects count went up), timeout 120 s. No scheduled ticker: nothing in the definition depends on time, so the free plan's daily cron limit does not matter. The function imports ../../src/engine.ts, the same runtime the local drainer uses. `blueprints plan` showed 2 creates, `blueprints deploy` took 80 s (robot created at :36, function created at :55). Webhook and site URL added with `sanity functions env add`, not in the committed blueprint.
- Serial 3 with no local drainer running: the Function was invoked 4 s after startInstance, ran Pedantry (demanded "a second copy, Regulation 3 subsection 2"), returned to intake. Resubmitted from the CLI; the Function then ran Pedantry (accepted) -> Rubber -> Archive, which lost it (serial 3, lostCount 0): receipt LOST-0003-1 written, lostCount incremented, back to intake, auto stamped in, Pedantry waved it through without a model call ("Previously inspected. Stamped again for good measure."), Rubber again, Archive filed it ("Shelf M, mildly resigned"), Minister's desk. 40 s wall clock from resubmit to the Minister, all in the cloud. 107 history entries on the instance. effectHistory actor is now the robot (g-D4cbqTOcxvM8, roles editor), so the audit trail distinguishes the clerks from the humans. ctx.log lines show up in `sanity functions logs` at ERROR level, which is odd but harmless.
- Left prod.wf-instance.7006e3208355 at the Minister's desk to approve from the back office once it exists.
- 03:0x to 04:0x App SDK back office (ministry/app). Scaffold `init --template app-quickstart` gave @sanity/sdk ^2 and sanity 6.15; Workflows needs sdk 3.1+ and @sanity/ui 3, so: sdk 3.x, ui 3.5.5, sanity 6.15 kept only as the CLI (its own ui 4 nests under it, no peer conflict because nothing renders it). First attempt with sanity 6.9.2 as the CLI never got past the Dashboard spinner: the 6.9 app runtime predates SDK 3 (Aug 11 vs Aug 27). @sanity/workflow-components 0.33.0 declares "types" but ships no .d.ts, so there is a 20 line local declaration for AssigneePicker and ProjectMembersState.
- Dashboard dev mode (`sanity dev` + ?dev=http://localhost:3334) showed a broken frame in this Chrome even for the untouched quickstart, so every check went through a real deploy (`sanity deploy --create --title "Ministry Back Office"`, app id itbdo7i85h2ygpi8pfojqvqn, about 40 s per deploy).
- Works: the corridor board (useWorkflowInstances, live: a form filed on the site appeared in the Pedantry column without a reload), the Staff register (useDocument + useEditDocument on the three clerks, temperament slider, publish button).
- Broke, unresolved: opening a file. Clicking a card with the original FileView (useWorkflowSession from @sanity/workflow-sdk) hung the app iframe every time, for the Minister's desk instance and for a completed one; the Dashboard top frame stayed responsive (site isolation), the iframe never painted again. Bisected with header checkboxes: still hung with the diagram, actions, assignee picker and history all switched off, so the session hook itself. Pinning @sanity/sdk to 3.1.0 changed nothing. The same engine evaluates the same instance in Node in 350 ms and engine.session() builds in 2 ms, so the engine and the definition are fine; it is the React adapter or the SDK observer. Could not read the iframe console: the app host URL bounces to the Dashboard home, the standalone localhost login needs a Google account chooser the browser extension may not drive. Rewrote FileView without the session: useDocument on the instance document (it is just a document) + engine.evaluate on each revision + engine.fireAction / engine.editField for the buttons. Then discovered the extension's clicks into the Dashboard iframe are themselves unreliable (a header checkbox and the Staff tab stopped responding in the same tab), so the rewritten view is deployed but unverified by me. Asked Aivaras to click a card in his browser.
- Definition v3: `minister` assignee field now `editable: true` (evaluate returned editableFields [] on v2, the picker had nothing to edit).
- Astro site (ministry/web): Astro 7.3, Vercel adapter, output server. Pages: / (form + corridor + counts), /status/[id] (live: the instance document read by GROQ, remarks, corridor with visits, resubmit and withdraw forms when the form is at the desk with a demand or rejection; polls /api/status/[id] every 4 s and reloads on a stage change), /wall, /lost, /about. API: /api/submit (creates a published submission with the next serial, engine.startInstance, 303 to the status page, 5 per 10 min per IP), /api/act (resubmit/withdraw via engine.fireAction as execution context server:ministry-site). All reads use the token: this project denies anonymous reads.
- End to end through the site with no local drainer: serial 4 filed at 03:4x, Pedantry demanded a reference number ("The serial number does not constitute a reference number."), resubmitted through the site form, 24 s later at the Minister's desk with all three remarks on the public page. Two files now wait for the Minister (serials 3 and 4).
- Text nit found: the status page says "A clerk is on it" at the Minister's desk. Should say the Minister. Fix pending.
- 04:2x Deployed: site https://ministry-of-approvals.vercel.app (Vercel project ministry-of-approvals, team manifestas-projects, SANITY_API_TOKEN as a production env var), Studio https://ministry-of-approvals.sanity.studio (appId u2dpkzwo172lj63g73giev8m, autoUpdates off on purpose so the hosted Studio cannot drift to 6.10+ and lose the Workflows plugin), back office app id itbdo7i85h2ygpi8pfojqvqn in the Dashboard. Production smoke: /, /wall, /lost, /about, /status/[id], /api/status/[id] all 200.
- Studio Workflows tool in the Dashboard lists both open runs and opens the run panel for serial 4: subject, fields, the stage chips, "Rule on the file: Select action", full history with the robot actor. That is the same reactive session (@sanity/workflow-react) running through the Studio adapter, on the same instance that hangs in the App SDK adapter. So the hang is in @sanity/workflow-sdk's observer path (or my use of it), not in the engine, the definition or the instance. Worth a bug report to Sanity with the instance id.
- Left for Aivaras: (1) open the back office, click a card, say whether it opens or hangs; (2) rule on serials 3 and 4 as the Minister (Studio Workflows tool or the back office); (3) once in a while a Studio "Start workflow" on a submission created in Studio to show autoStart.
- 10:2x Aivaras confirmed the back office file view opens in his browser; then it opened for me too in a fresh tab (the earlier "hang" observations were at least partly the browser extension losing the tab). Approved serial 3 from the back office with a note: the dialog fired the action, the Function issued MoA-2026-0003 and posted to Discord within 10 s, the file moved to On the Wall and the audit trail grew from 107 to 127 entries live. Aivaras had already approved serial 4 ("test") from his side.
- Fixes: certificate stamps for a lost form are now two stamps (lost, with the Archivist's excuse from the receipt; then filed), certificate 0003 patched by hand to match; the wall says "A form" instead of "A other"; the back office audit trail names the runtime from executionContext (the clerks (Function), the Front Desk (site), an agent) instead of "a person" for the robot; diagram box 380 px. Function, site and app redeployed. Three certificates on the wall, one receipt in the hall.
- Note for the writeup: instances pinned to definition v2 do not show the Minister assignee picker (the field became editable in v3); new forms will.
- 13:3x Aivaras: raise the bar, do everything that makes it a winner. Plan: workflow depth first (guards, Department of Delays, bench tests), then a real visual identity for the site with the certificate as a rendered object, meme uploads, clerk portraits from Agent Actions, then the MCP agent recording.
- Definition v4: guards on the four holding stages (freeze title/kind/body/citizen with a delta predicate; deny delete), names had to be unique per definition so they are suffixed by stage. The Minister's desk now has a stage scoped `deadline` datetime seeded by a GROQ query value (`string(dateTime(now()) + 3600)`), a trigger `sat-too-long` (`$fields.deadline <= $now`) that sets decision overdue, flags `overdue` and increments `delays`, and a transition to the Archive. The archivist loses an overdue form regardless of serial, says so, clears the flag. `delays` rides onto the certificate.
- Bench tests (`@sanity/workflow-engine-test`, vitest): 14 tests, the corridor, the lost loop, withdraw, clerk failure escalation, approve/announce/wall, announce failure still hangs the certificate, reject, refer, the Delays path with the clock advanced two hours, guards deny an edit and a delete while a department holds the form and lift at the desk. Two things the tests caught: approve without a note threw (field.set refuses an undefined param, the back office allowed an empty note) so the note is now required; and the deadline query value resolves on the in memory lake's wall clock, not the bench clock, so that one test runs on the wall clock. All 14 green in 0.6 s.
- Runtime: Blueprint stack promoted to organization scope (`blueprints promote`), new Scheduled Function `wf-prod-tick-instances` hourly (Growth's fastest) that sweeps stale claims and ticks every in flight instance; the drainer was recreated during promotion and kept its env vars. Local test run: "ticked 0 instance(s)". The site's status poll also ticks a file while it sits at the Minister's desk so a demo does not wait for the cron.
- 13:4x to 16:2x Visual identity. Wrote ministry/DESIGN.md first (the Ministry is a building, you hold real paper; references: Wes Anderson title cards, 1970s ministry forms, rubber stamp physics, diploma engraving, Flighty's state chips). Then rebuilt the site: plaster wall with a green dado, a corridor of SVG doors drawn from the department documents (colour, motto, brass plate with the clerk's name, frosted glass), a paper sprite that slides to the current door, doors stamped SEEN xN after visits, stamps with an ink mask and a slam animation on state change, forms as punched paper sheets with a boxed serial, typed text in carbon blue, receipts in a steel filing cabinet with torn edges, certificates in brass frames hung crooked with plaques.
- Certificate as an object: Certificate.astro (guilloche border, corner ornaments, four stamps overlapping, seal, signature line, serial punched on the edge) and the same layout rendered to a 1200x800 PNG by satori + resvg-wasm at /api/certificate/[id].png. Fonts fetched as TTF from Google Fonts (the IE6 user agent gives EOT, an old Safari UA gives TrueType). Broke on Vercel twice: resvg's wasm, then satori's yoga.wasm and harfbuzz wasm were not in the function bundle; fixed with the adapter's includeFiles. The Discord announcement now embeds the PNG, so the "external API call" shows the actual document. First render 1.2 s locally, 2.9 s cold on Vercel, cached after.
- Meme uploads: the form takes an image (4 MB cap, png/jpeg/gif/webp), the API route uploads it to the Content Lake with client.assets.upload before creating the submission, and it shows on the status page and the certificate. Tested from production with the certificate PNG itself as Exhibit A (serial 5, "mildly self referential" says the Archivist). Astro's CSRF check refuses a POST without an Origin header, which is correct and bit curl first.
- Clerk portraits: a Studio document action on clerk documents calls Agent Actions generate with target portrait.asset and forcePublishedWrite, prompt built from the clerk's own system prompt (1970s civil service ID photo turned oil portrait). Ran it for all three: 3 AI credits, about 40 s each, asynchronous. Ms. Regulation Ona has a red pen, Mr. Approvals Tomas has one enormous stamp, the Archivist is three floors underground. Portraits appear on the Front Desk staff panel, in the door windows, and in the back office staff cards.
- Broke: the browser extension's screenshots kept timing out on my own pages after a scroll. Real cause, not the extension: a fixed full viewport SVG feTurbulence texture plus an feTurbulence mask on every stamp, plus a rotated form full of inputs and a textarea with background-attachment local. Pre rendered the grain and ink tiles to PNG with resvg, dropped the rotations on anything with inputs, put the grain on the html background. Captures went back to instant. Worth knowing for the writeup: live SVG filters as page textures are a repaint tax.
- Social preview: public/og.png rendered with satori (also the post cover). README, MIT licence, DESIGN.md. Repo public at https://github.com/A1VARA5/ministry-of-approvals (91 files, no secrets; web/.env.example was ignored by the template's .env.* rule until a negation was added).
- Serial 5 sits at the Minister's desk with a one hour deadline. Left on purpose: the Department of Delays should take it on the next tick after the deadline (the site polls while the status page is open; the hourly Function otherwise).
- 16:3x to 17:0x Submission pack. Recorded the corridor walk as a GIF (30 frames, serial 6: filed, returned by Pedantry with "Regulation 14 subsection 2, every declaration concerning the status of office flora must be filed in duplicate", resubmitted, Rubber Stamps, lost by the Archive, second lap with SEEN x3 on the Front Desk door, Minister's desk with the deadline printed). Seven screenshots in 04-submission/screenshots/path-two.
- Broke during the recording: the Document Function did not pick up serial 6's first effect for three minutes (earlier runs: 4 seconds). The event did arrive, late; my local drainer had already taken the effect, so the Function logged "drained 0". Sanity event delivery latency, not a filter bug. Added a fallback drainer to the site: the status poll runs any effect nobody has claimed for 30 s (same handlers, vendored into web/src/effects by a sync script because Vercel uploads only the web folder; the Vercel project root could not be changed from here, the MCP token lacks the permission and the CLI token refuses raw API calls). Both runtimes claim under a lease. Serial 7 was picked up by the Function in under 10 s.
- Broke: the AssigneePicker from @sanity/workflow-components hung the Dashboard iframe when wired to engine.editField (editField itself takes 3.4 s from Node and works). Replaced with a "Take this file" / "Release it" button on the current user. The empty file panel now collapses so the board gets the width.
- Fixed "In the The Archive" on the status page.
- Post drafted in 04-submission/path-two-post.md (2,100 words, voice linted: the only hits are file names and the template's own headings). Redacted transcript copy for the agent session upload in 04-submission/agent-session/ (none of the nine stored secrets appear in this session; four Dashboard session tokens and four Vercel token fragments redacted). BUILD-LOG copied into the repo.
- 17:0x to 17:3x (14:0x to 14:3x UTC) Submission pack, second half. Recorded the back office ruling as a GIF (34 frames, serial 7 "Install a second lift that only goes down": board, file open with the diagram and amendments, Take this file, Approve with a note, audit trail growing live through Certifying to On the Wall, about 15 s). The extension's clicks into the Dashboard iframe need a double click with a one second gap; a single click is swallowed. Scrolling over the diagram zooms it instead of the panel, scroll on the text. Certificate MoA-2026-0007 PNG and the Discord embed saved. Media committed to the repo under docs/media (GIFs downscaled to 1100 px and 128 colours: 14 MB to 9 MB and 8 MB to 4 MB) and linked from raw.githubusercontent.com.
- Filed serial 8 ("Permission for the Minister to have lunch") to prove the Department of Delays in production; Pedantry wanted a filing reference number, resubmitted via /api/act, at the Minister's desk at 14:11 UTC. Serial 6 has been at the desk since 13:41:51 UTC. Watching both past their deadlines.
- The post rewritten in Aivaras's voice (Register A, voice lint clean apart from the template's own headings and the `![` image syntax that the lint counts as exclamation marks). Agent session: the raw transcript is 29 MB and DEV's uploader takes 10 MB, so a trimmed copy (tool results cut to 1,500 chars, images dropped, non message records removed, 5 MB) was uploaded at dev.to/agent_sessions/new; 715 messages parsed, DEV redacted 6 home directory paths, I excluded the compaction summary and the voice skill load (private notes about him). Public slug the-ministry-of-approvals-a-workflow-that-loses-your-form-on-purpose-cl41vw, embedded in the post. DEV draft created through the API (article 4708608): https://dev.to/manifesta/i-built-a-ministry-that-loses-your-form-on-purpose-3fej-temp-slug-9220887 . First API attempt was refused with 422 because the embed placeholder was not a valid agent_session tag; the session had to exist first. `04-submission/push-devto-draft.py <post.md> <id>` re pushes the draft.
